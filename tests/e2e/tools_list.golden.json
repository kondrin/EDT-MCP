[
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Adopt a base-configuration metadata object or member (object / form / attribute / tabular section / ...) into a configuration EXTENSION so the extension can override or intercept it - the MCP equivalent of EDT's 'Add To Extension'. Addressed by the base object FQN; pass extensionProjectName when more than one extension extends the configuration. Adopting BSL code/methods is NOT covered. Full parameters and examples: call get_tool_guide('adopt_metadata_object').",
    "inputSchema": {
      "properties": {
        "extensionProjectName": {
          "description": "Target extension EDT project name; REQUIRED only when more than one extension extends the configuration (otherwise the single extension is used automatically)",
          "type": "string"
        },
        "fqn": {
          "description": "Full-name FQN of the object or member to adopt (required), e.g. 'Catalog.Products', 'Catalog.Products.Attribute.Weight', 'Catalog.Products.Form.ItemForm'",
          "type": "string"
        },
        "projectName": {
          "description": "The BASE configuration EDT project that owns the object (NOT the extension) (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "fqn"
      ],
      "type": "object"
    },
    "name": "adopt_metadata_object",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "'adopted' or 'alreadyAdopted'",
          "type": "string"
        },
        "extensionProject": {
          "description": "The extension the object was adopted into",
          "type": "string"
        },
        "fqn": {
          "description": "FQN of the adopted object in the extension",
          "type": "string"
        },
        "objectBelonging": {
          "description": "ADOPTED (the object is now an adopted copy)",
          "type": "string"
        },
        "persisted": {
          "description": "Whether the change was exported to disk",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Clean EDT project and trigger full revalidation. Refreshes files from disk, clears all validation markers, and waits for EDT to complete revalidation.",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "Name of the project to clean (optional, cleans all EDT projects if not specified)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "clean_project",
    "outputSchema": {
      "properties": {
        "message": {
          "description": "Human-readable completion message",
          "type": "string"
        },
        "projects": {
          "description": "Names of the projects that were cleaned",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "projectsCleaned": {
          "description": "Number of projects that were cleaned",
          "type": "integer"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Create a new FILE infobase (1C database) OR register an existing one, and bind it to a configuration project so it appears in get_applications. mode='create' (default) makes a new database (requires a registered 1C platform runtime); mode='register' adds an already-existing infobase at the given path without launching the platform. applicationKind='infobase' (default) makes a plain file infobase; applicationKind='standaloneServer' creates an autonomous (standalone) server that also exposes a web URL for HTTP testing (requires a registered 1C standalone-server runtime, platform >= 8.3.23). FILE type only (server/web rejected). Runs in a background Job (up to 120 s). Full parameters and examples: call get_tool_guide('create_infobase').",
    "inputSchema": {
      "properties": {
        "access": {
          "description": "Authentication kind for the stored credentials: 'INFOBASE' (default, 1C user auth) or 'OS'. Credentials are stored when ANY of user/password/access is given; access='OS' on its own stores OS-authentication settings (no 1C user/password). Applies only to applicationKind='infobase' (a file infobase).",
          "enum": [
            "INFOBASE",
            "OS"
          ],
          "type": "string"
        },
        "applicationKind": {
          "description": "'infobase' (default) = a plain file infobase via the configurator; 'standaloneServer' = an autonomous (standalone) server that creates and serves a new file infobase and exposes a web URL for HTTP testing (requires a registered 1C standalone-server runtime, platform >= 8.3.23). The web port is auto-allocated by EDT and reported back as 'port'/'webUrl' in the result.",
          "enum": [
            "infobase",
            "standaloneServer"
          ],
          "type": "string"
        },
        "infobaseFile": {
          "description": "Absolute path to the infobase directory (required). For mode='create' the directory is created if absent and the 1Cv8.1CD files are written into it; for mode='register' it must already contain an existing file infobase.",
          "type": "string"
        },
        "infobaseName": {
          "description": "Display name for the new infobase. If omitted, a name is auto-generated by EDT.",
          "type": "string"
        },
        "mode": {
          "description": "'create' (default) = make a new file infobase at infobaseFile (launches the 1C platform); 'register' = add an EXISTING infobase already present at infobaseFile (no platform launch).",
          "enum": [
            "create",
            "register"
          ],
          "type": "string"
        },
        "password": {
          "description": "Password for 'user'. Optional; default empty (demo bases use an empty password).",
          "type": "string"
        },
        "platform": {
          "description": "1C platform version mask to use for creation (e.g. '8.3.25'). If omitted, EDT resolves the best available installed version automatically.",
          "type": "string"
        },
        "projectName": {
          "description": "EDT configuration project to bind the new infobase to (required).",
          "type": "string"
        },
        "setDefault": {
          "description": "Set the new infobase as the default application for the project after creation (default false).",
          "type": "boolean"
        },
        "user": {
          "description": "Infobase connection user to store so update_database / debug_launch can authenticate the update agent (issue #194). Selects an EXISTING user; most useful with mode='register' (the existing base already has users). Omit to store no credentials.",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "infobaseFile"
      ],
      "type": "object"
    },
    "name": "create_infobase",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "'created' (mode=create) or 'registered' (mode=register).",
          "type": "string"
        },
        "applicationId": {
          "description": "ID of the newly created application (for chaining into update_database).",
          "type": "string"
        },
        "applicationKind": {
          "description": "'infobase' or 'standaloneServer' — the kind of application created.",
          "type": "string"
        },
        "applications": {
          "description": "Applications bound to the project after creation (same shape as get_applications).",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "infobaseFile": {
          "description": "Path of the created infobase directory.",
          "type": "string"
        },
        "infobaseName": {
          "description": "Display name of the created infobase.",
          "type": "string"
        },
        "message": {
          "description": "Human-readable status message.",
          "type": "string"
        },
        "port": {
          "description": "applicationKind='standaloneServer' only: the ACTUAL web port the server was published on (parsed from webUrl; EDT auto-allocates it). Absent if webUrl could not be resolved.",
          "type": "integer"
        },
        "project": {
          "description": "Name of the configuration project.",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "webUrl": {
          "description": "applicationKind='standaloneServer' only: the infobase web URL for HTTP testing. Best-effort: absent if EDT could not resolve the URL (the server is still created).",
          "type": "string"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Create a 1C:EDT runtime-client launch configuration (thin/thick/web). The SAME config works for both run and debug (mode is chosen at launch time by debug_launch/run_yaxunit_tests — there is no separate debug-config type). Use delete_launch_config to remove it. Full parameters and examples: call get_tool_guide('create_launch_config').",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application (infobase) ID from get_applications. If omitted the project's default application is used. If the project has no applications, the call is rejected with a hint.",
          "type": "string"
        },
        "clientType": {
          "description": "Client kind: thin (default), thick, or web.",
          "enum": [
            "thin",
            "thick",
            "web"
          ],
          "type": "string"
        },
        "name": {
          "description": "Config name; default '<Project> Thin|Thick|Web Client', uniquified. If a config with this name already exists the call is rejected.",
          "type": "string"
        },
        "projectName": {
          "description": "V8 configuration project name (required). Must be a V8ConfigurationNature project. Use list_projects to discover available projects.",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "create_launch_config",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "Always 'created' on success.",
          "type": "string"
        },
        "applicationId": {
          "description": "Application ID stored in the config (real IApplicationManager id).",
          "type": "string"
        },
        "clientType": {
          "description": "Client kind: thin, thick, or web.",
          "type": "string"
        },
        "message": {
          "description": "Human-readable status message.",
          "type": "string"
        },
        "name": {
          "description": "Exact name of the created launch configuration.",
          "type": "string"
        },
        "project": {
          "description": "Project the configuration targets.",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "type": {
          "description": "Launch configuration type id.",
          "type": "string"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Create a metadata node addressed by a 1C full-name FQN: a top-level object (Catalog.Products) or a subordinate member (Catalog.Products.Attribute.Weight, InformationRegister.Prices.Dimension.Product, Enum.Colors.EnumValue.Red). The kind is inferred from the FQN; type and kind tokens may be English or Russian. Full parameters and examples: call get_tool_guide('create_metadata').",
    "inputSchema": {
      "properties": {
        "callType": {
          "description": "Form event handler ONLY (item-level '...Form.F.<ItemKind>.Item.Handler.<Event>' or form-level '...Form.F.Handler.<Event>'), in a configuration EXTENSION project. Selects EXTENSION event interception: binds a form:EventHandlerExtension with this call type instead of a plain base handler, so the extension reacts Before / After / Instead of the base element's event (works even when the base element has no handler of its own). Omit for a normal base handler. The BSL handler procedure itself is added separately via write_module_source. Rejected on a base configuration or a non-handler FQN.",
          "enum": [
            "Before",
            "After",
            "Instead"
          ],
          "type": "string"
        },
        "commonModuleKind": {
          "description": "CommonModule top-object only. Selects a standards-compliant flag combination the common-module-type validator accepts (no warning), instead of a bare module: 'Server', 'ServerCall', 'ClientManaged', 'ClientOrdinary', 'ClientServer', 'Global'. Defaults to 'Server'. Ignored for other types. Combine with 'serverCall' / 'privileged' / 'returnValuesReuse'. These are create-time-only (the flag set cannot be re-derived post-hoc).",
          "enum": [
            "Server",
            "ServerCall",
            "ClientManaged",
            "ClientOrdinary",
            "ClientServer",
            "Global"
          ],
          "type": "string"
        },
        "expectedNotExists": {
          "description": "Optional stale-intent guard (default false): assert the node does not yet exist for a sharper precondition error. A real duplicate is always rejected anyway.",
          "type": "boolean"
        },
        "fqn": {
          "description": "Full-name FQN of the node to create (required). Top object: 'Type.Name' (e.g. 'Catalog.Products'). Member: 'Type.Name.Kind.Name' (e.g. 'Catalog.Products.Attribute.Weight'). The trailing Name is the new node's programmatic Name; type / kind tokens may be English or Russian.",
          "type": "string"
        },
        "normalizeYo": {
          "description": "Normalize the Russian letter 'ё'->'е' / 'Ё'->'Е' in the new node's NAME (the trailing FQN segment) and in any synonym / comment value (default true). 'ё' in a Name is flagged by the 1C standard mdo-ru-name-unallowed-letter, so normalizing on input stores a compliant name. Set false to keep 'ё' exactly as supplied.",
          "type": "boolean"
        },
        "privileged": {
          "description": "CommonModule top-object only. When true, the module runs with full (privileged) access. Valid only with the 'Server' kind (not a server call). Ignored for other types.",
          "type": "boolean"
        },
        "projectName": {
          "description": "EDT project name (required).",
          "type": "string"
        },
        "properties": {
          "description": "Optional properties to apply at creation, as [{name, value, language?}]. This version applies 'synonym' (with optional 'language' code) and 'comment'; other property names are rejected (set them via modify_metadata).",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "returnValuesReuse": {
          "description": "CommonModule top-object only. Reuse of return values: 'DontUse' (default), 'DuringRequest' or 'DuringSession'. 'DuringSession' yields a cached module accepted by the common-module-type validator. Ignored for other types.",
          "enum": [
            "DontUse",
            "DuringRequest",
            "DuringSession"
          ],
          "type": "string"
        },
        "serverCall": {
          "description": "CommonModule top-object only. When true, the server module is callable from the client (server call). Valid only with a server kind and incompatible with 'Global'. Ignored for other types.",
          "type": "boolean"
        },
        "setAsDefault": {
          "description": "Form OBJECT create only (FQN 'Type.Object.Form.FormName'). When true, registers the new form as the owner's default object form (default: false). Ignored for other create kinds.",
          "type": "boolean"
        },
        "targetNamespace": {
          "description": "XDTOPackage top-object only. URI namespace for the new package; a non-empty namespace is required for the package to be valid. Defaults to 'http://example.org/<Name>' when omitted. Create-time-only. Ignored for other types.",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "fqn"
      ],
      "type": "object"
    },
    "name": "create_metadata",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "'created' on success",
          "type": "string"
        },
        "callType": {
          "description": "Extension event call type written (Before/After/Instead), when an extension event handler (form:EventHandlerExtension) was created",
          "type": "string"
        },
        "commonModuleKind": {
          "description": "Resolved CommonModule kind, when a CommonModule was created",
          "type": "string"
        },
        "fqn": {
          "description": "Normalized full-name FQN of the created node",
          "type": "string"
        },
        "kind": {
          "description": "EClass of the created node (e.g. 'Catalog', 'CatalogAttribute')",
          "type": "string"
        },
        "language": {
          "description": "Language code the synonym was written for",
          "type": "string"
        },
        "message": {
          "description": "Human-readable confirmation message",
          "type": "string"
        },
        "name": {
          "description": "Programmatic name of the created node",
          "type": "string"
        },
        "normalized": {
          "description": "Fields whose value was rewritten by the 'ё'->'е' normalization (when any)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "persisted": {
          "description": "Whether the change was exported to disk",
          "type": "boolean"
        },
        "setAsDefault": {
          "description": "Whether the new form was registered as the owner's default object form (form-object create only)",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the node was created",
          "type": "boolean"
        },
        "synonym": {
          "description": "Display name written, when a synonym property was provided",
          "type": "string"
        },
        "targetNamespace": {
          "description": "XDTO namespace written, when an XDTOPackage was created",
          "type": "string"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Create a NEW 1C project in the EDT workspace. projectKind selects the kind: 'configuration' (standalone), 'extension' (bound to a base configuration), or 'externalObjects' (external data processors/reports). The name must not already exist as a project. standardChecks/commonChecks are applied only when com.e1c.v8codestyle is installed. Full parameters and examples: call get_tool_guide('create_project').",
    "inputSchema": {
      "properties": {
        "autoSortTopObjects": {
          "description": "Reserved for future use: auto-sort top-level metadata objects. Accepted but not yet applied in this release (see codestyle.autoSortNote in the response).",
          "type": "boolean"
        },
        "baseProjectName": {
          "description": "Name of the BASE configuration EDT project (required for extension; REJECTED for configuration and externalObjects). Must be an existing, open V8 configuration project. Use list_projects to find it.",
          "type": "string"
        },
        "comment": {
          "description": "Optional free-text comment set on the Configuration (configuration and extension only; REJECTED for externalObjects).",
          "type": "string"
        },
        "commonChecks": {
          "description": "Enable common (project-level) BSL checks for the new project (default true). Applied to all kinds only when com.e1c.v8codestyle is installed; ignored otherwise.",
          "type": "boolean"
        },
        "compatibilityMode": {
          "description": "Optional extension compatibility-mode string matching a CompatibilityMode enum literal (e.g. 'Version8_3_10'); empty = factory default. Unknown values are rejected. Extension only; REJECTED for other kinds.",
          "type": "string"
        },
        "name": {
          "description": "Name of the new Configuration object (required). For configuration/extension: the programmatic Configuration name. For externalObjects: used as the default project name (no Configuration object exists). Must be a valid 1C identifier: starts with a letter or underscore, then letters, digits and underscores only (Cyrillic allowed). Also used as the default EDT project name if projectName is not supplied.",
          "type": "string"
        },
        "prefix": {
          "description": "NamePrefix for the extension (extension only; REJECTED for other kinds). Default: empty string. The wizard generates a value like 'Ext1_'; pass an explicit value or omit for empty.",
          "type": "string"
        },
        "projectKind": {
          "description": "Kind of project to create (required): 'configuration' = standalone 1C configuration; 'extension' = configuration extension bound to a base project; 'externalObjects' = external data processors/reports project.",
          "enum": [
            "configuration",
            "extension",
            "externalObjects"
          ],
          "type": "string"
        },
        "projectName": {
          "description": "EDT workspace project name to create. Default: extension -> '<baseProjectName>.<name>'; configuration/externalObjects -> 'name'.",
          "type": "string"
        },
        "purpose": {
          "description": "Extension purpose (extension only; REJECTED for other kinds). Default: Customization. Customization = user adaptation; AddOn = add-on functionality; Patch = hotfix.",
          "enum": [
            "Customization",
            "AddOn",
            "Patch"
          ],
          "type": "string"
        },
        "scriptVariant": {
          "description": "Script variant (Russian or English). configuration: sets the Configuration scriptVariant and default Language code (default Russian). externalObjects: applied post-create via setScriptVariant (non-fatal on failure). extension: REJECTED — scriptVariant is always inherited from the base configuration.",
          "enum": [
            "Russian",
            "English"
          ],
          "type": "string"
        },
        "standardChecks": {
          "description": "Enable 1C:Standards BSL checks for the new project (default true). Applied to all kinds only when com.e1c.v8codestyle is installed; ignored otherwise.",
          "type": "boolean"
        },
        "synonym": {
          "description": "Human-readable synonym for the Configuration (configuration and extension only; REJECTED for externalObjects). Defaults to the 'name' value if omitted.",
          "type": "string"
        },
        "version": {
          "description": "Platform version string, e.g. '8.3.27' (configuration and externalObjects only; for extension: REJECTED — version is always inherited from the base configuration). Default: Version.LATEST when omitted.",
          "type": "string"
        }
      },
      "required": [
        "projectKind",
        "name"
      ],
      "type": "object"
    },
    "name": "create_project",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "'created' on success",
          "type": "string"
        },
        "baseProject": {
          "description": "Name of the base configuration project it extends (extension only, conditional)",
          "type": "string"
        },
        "codestyle": {
          "description": "v8codestyle preference application result: {applied: bool, note: string, autoSortNote: string}",
          "type": "object"
        },
        "message": {
          "description": "Human-readable confirmation message",
          "type": "string"
        },
        "name": {
          "description": "Configuration name (configuration/extension) or the supplied identifier (externalObjects).",
          "type": "string"
        },
        "prefix": {
          "description": "NamePrefix applied (extension only, conditional)",
          "type": "string"
        },
        "project": {
          "description": "EDT workspace project name of the created project (round-trip key for sibling tools)",
          "type": "string"
        },
        "projectKind": {
          "description": "Kind of project created: configuration, extension, or externalObjects",
          "type": "string"
        },
        "purpose": {
          "description": "ConfigurationExtensionPurpose value applied (extension only, conditional)",
          "type": "string"
        },
        "scriptVariant": {
          "description": "ScriptVariant applied or inherited",
          "type": "string"
        },
        "scriptVariantNote": {
          "description": "Present only when a requested scriptVariant could not be applied (externalObjects).",
          "type": "string"
        },
        "state": {
          "description": "'ready' when lifecycle STARTED was reached, 'created' otherwise",
          "type": "string"
        },
        "success": {
          "description": "Whether the project was created",
          "type": "boolean"
        },
        "synonymNote": {
          "description": "Present only when the synonym could not be applied (no resolvable language code).",
          "type": "string"
        },
        "version": {
          "description": "Platform version string used for project creation",
          "type": "string"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Start an EDT debug session: either an existing config by launchConfigurationName (runtime client OR Attach, the latter needed to debug server-side code), or a runtime-client config matched by projectName + applicationId. If that config is already running it short-circuits with alreadyRunning:true; pass restartIfRunning:true to terminate the existing session and relaunch fresh. Full parameters and examples: call get_tool_guide('debug_launch').",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application ID from get_applications; required in the projectName+applicationId mode.",
          "type": "string"
        },
        "launchConfigurationName": {
          "description": "Exact name of an EDT debug launch config (runtime client or Attach); skips projectName/applicationId.",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name; required unless launchConfigurationName is given.",
          "type": "string"
        },
        "restartIfRunning": {
          "description": "Default false: if a matching session is already running, short-circuit with alreadyRunning:true and do NOT relaunch (call terminate_launch to restart). true: non-interactively terminate the existing session, then relaunch — no 'Debug session already exists' modal blocks the call.",
          "type": "boolean"
        },
        "updateBeforeLaunch": {
          "description": "Default true: silently apply the configuration->DB update before launching so no 'Update database?' modal blocks the call (even on a Russian-locale EDT the dialog is auto-confirmed); false skips the update and the platform may then show that modal. Ignored for Attach.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "debug_launch",
    "outputSchema": {
      "properties": {
        "alreadyRunning": {
          "description": "True if a matching session was already alive; re-launch skipped",
          "type": "boolean"
        },
        "applicationId": {
          "description": "Application id of the launched configuration",
          "type": "string"
        },
        "attach": {
          "description": "True if this is an Attach (server-side debug) configuration",
          "type": "boolean"
        },
        "configurationType": {
          "description": "Launch configuration type id",
          "type": "string"
        },
        "launchConfiguration": {
          "description": "Name of the launched/running launch configuration",
          "type": "string"
        },
        "message": {
          "description": "Human-readable status message",
          "type": "string"
        },
        "mode": {
          "description": "Launch mode of the session (e.g. debug, run)",
          "type": "string"
        },
        "project": {
          "description": "EDT project name associated with the launch",
          "type": "string"
        },
        "status": {
          "description": "\"launching\" when the launch was dispatched asynchronously and is still starting; absent on the alreadyRunning short-circuit. Poll debug_status for readiness.",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Report active debug sessions: applicationId (real or synthetic 'attach:<name>' / 'launch:<name>'), launch configuration name/type, mode (debug/run), whether the target is currently suspended, thread count, and the line of the top suspended frame. Also reports debugServerTargets: 1C debug-server sessions (server-side suspends, EDT-UI-started 'Debug As') addressable as 'ServerApplication.<app>'. Optionally filter by applicationId.",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Optional application id filter",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "debug_status",
    "outputSchema": {
      "properties": {
        "count": {
          "description": "Number of active debug launches returned",
          "type": "integer"
        },
        "debugServerTargetCount": {
          "description": "Number of debug-server targets returned",
          "type": "integer"
        },
        "debugServerTargets": {
          "description": "1C debug-server targets (server-side / EDT-UI sessions) with real suspend state",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "launches": {
          "description": "Active debug launches with state and frame info",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "registry": {
          "description": "Debug session registry snapshot counters",
          "type": "object"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Deprecated alias for run_yaxunit_tests with debug=true. Launches YAXUnit tests in DEBUG mode so breakpoints fire, then call wait_for_break to inspect. Prefer run_yaxunit_tests(debug=true) — identical behaviour. Full parameters and examples: call get_tool_guide('debug_yaxunit_tests').",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application id from get_applications (required if launchConfigurationName is omitted).",
          "type": "string"
        },
        "extensions": {
          "description": "Comma-separated extension names to filter tests by extension.",
          "type": "string"
        },
        "launchConfigurationName": {
          "description": "Exact runtime-client launch config name (preferred; from list_configurations).",
          "type": "string"
        },
        "modules": {
          "description": "Comma-separated module names to filter tests.",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required if launchConfigurationName is omitted).",
          "type": "string"
        },
        "tests": {
          "description": "Comma-separated test names as Module.Method (recommended: pin to one test for a predictable cycle).",
          "type": "string"
        },
        "updateBeforeLaunch": {
          "description": "Default true: terminate any live client and run a silent DB update first so no modal 'Update database?' dialog blocks the call; false keeps legacy delegate behaviour — no client sweep, no auto-confirmed update dialog; platform dialogs may appear.",
          "type": "boolean"
        },
        "updateScope": {
          "description": "Which projects to rebuild+update before the run: 'all' (configuration + dependent extensions, default), 'configuration', or 'extension:<ProjectName>' (comma-separate several). Forces a derived-data recompute so a freshly edited extension's .cfe is regenerated and loaded into the infobase before the run. Unknown extension names fail the call (the error lists the available names). Only applies when updateBeforeLaunch=true.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "debug_yaxunit_tests",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": true,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Remove a FILE infobase association from a configuration project OR delete a standalone (autonomous) server application. Destructive: guarded by a confirm-preview - call without confirm to preview what would be removed (no change), then confirm=true to delete. For a file infobase: dissociates it and (deleteRegistration, default true) deregisters it from the EDT infobases list. For a standalone server (applicationKind=standaloneServer): stops it and removes the WST server and its server config folder. By default the infobase DATABASE FILES on disk are KEPT (both kinds); pass deleteDatabaseFiles=true to also delete the database directory. The inverse of create_infobase. Full parameters and examples: call get_tool_guide('delete_infobase').",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application ID from get_applications. Either applicationId or infobaseName is required.",
          "type": "string"
        },
        "confirm": {
          "description": "true = perform the removal; default false = preview only (no change).",
          "type": "boolean"
        },
        "deleteDatabaseFiles": {
          "description": "true = ALSO delete the infobase database files/directory from disk (the 1Cv8.1CD directory) — IRREVERSIBLE. Default false = keep the database files on disk (the registration is removed but the data stays). Works for both a file infobase and a standalone server (deletes the served database directory).",
          "type": "boolean"
        },
        "deleteRegistration": {
          "description": "true = also deregister the infobase from the global EDT infobases list (equivalent to 'Delete' in the Infobases view); default true.",
          "type": "boolean"
        },
        "infobaseName": {
          "description": "Display name of the infobase to remove. Either applicationId or infobaseName is required.",
          "type": "string"
        },
        "projectName": {
          "description": "EDT configuration project the infobase is bound to (required).",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "delete_infobase",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "Either 'preview' (nothing changed) or 'deleted' (removed).",
          "type": "string"
        },
        "applicationId": {
          "description": "Application ID that was removed.",
          "type": "string"
        },
        "applicationKind": {
          "description": "'infobase' or 'standaloneServer' — the kind of application removed (standalone-server deletions only).",
          "type": "string"
        },
        "confirmationRequired": {
          "description": "true on a preview (no change made); absent/false once deleted.",
          "type": "boolean"
        },
        "databaseFilesDeleted": {
          "description": "Whether the database files on disk were actually deleted (only when deleteDatabaseFiles=true; false otherwise or if the directory could not be removed).",
          "type": "boolean"
        },
        "deleteRegistration": {
          "description": "Whether the EDT registry entry was (or would be) removed: the global infobases-list entry for a file infobase, or the infobases.yaml registry entry for a standalone server.",
          "type": "boolean"
        },
        "infobaseName": {
          "description": "Display name of the removed infobase.",
          "type": "string"
        },
        "message": {
          "description": "Human-readable status message.",
          "type": "string"
        },
        "project": {
          "description": "Name of the configuration project.",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": true,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Delete a 1C:EDT launch configuration by name (runtime client or Attach). Destructive: guarded by a confirm-preview - call without confirm to preview (no change), then confirm=true to delete. Refuses to delete a running config (terminate_launch first). The inverse of create_launch_config. Full parameters and examples: call get_tool_guide('delete_launch_config').",
    "inputSchema": {
      "properties": {
        "confirm": {
          "description": "true = perform the deletion; default false = preview only (what would be removed, no change).",
          "type": "boolean"
        },
        "name": {
          "description": "Exact launch configuration name (required). Use list_configurations to find available names.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "delete_launch_config",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "Either 'preview' (nothing changed) or 'deleted' (removed).",
          "type": "string"
        },
        "confirmationRequired": {
          "description": "true on a preview (no change made); absent/false once deleted.",
          "type": "boolean"
        },
        "message": {
          "description": "Human-readable status message.",
          "type": "string"
        },
        "name": {
          "description": "Name of the targeted launch configuration.",
          "type": "string"
        },
        "project": {
          "description": "Project the configuration targets (if known).",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "type": {
          "description": "Launch configuration type id (if known).",
          "type": "string"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": true,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Delete a metadata node (object or member, including a FORM object 'Type.Object.Form.Name' or a FORM member - item / attribute / command / handler) addressed by a 1C full-name FQN, cascading the cleanup of all references in BSL code, forms and other metadata. Two-phase: call without confirm to preview what would be removed, then confirm=true to apply (deletion is hard to reverse). If the node is still referenced by metadata the refactoring cannot auto-clean, a confirm=true delete is BLOCKED and the referencing objects are listed; pass force=true to delete anyway (those references are left dangling). Full parameters and examples: call get_tool_guide('delete_metadata').",
    "inputSchema": {
      "properties": {
        "confirm": {
          "description": "true = execute the deletion; default false = preview only.",
          "type": "boolean"
        },
        "force": {
          "description": "true = delete even when the node is still referenced by other metadata that the refactoring cannot auto-clean (those incoming references are left dangling). Default false = on confirm=true the deletion is BLOCKED and the referencing objects are listed (independent of 'confirm', which is the preview gate).",
          "type": "boolean"
        },
        "fqn": {
          "description": "Full-name FQN of the node to delete (required), e.g. 'Catalog.Products' or 'Document.SalesOrder.Attribute.Amount' (type / kind tokens may be English or Russian; the Name parts are the programmatic Name, not the synonym).",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required).",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "fqn"
      ],
      "type": "object"
    },
    "name": "delete_metadata",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "Either 'preview', 'executed' or 'blocked'",
          "type": "string"
        },
        "affectedReferences": {
          "description": "Deprecated alias of blockingReferences (the same list), kept for one release for wire compatibility",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "affectedReferencesCount": {
          "description": "Deprecated alias of blockingReferencesCount (the same count), kept for one release for wire compatibility",
          "type": "integer"
        },
        "blocking": {
          "description": "Whether the listed blockingReferences BLOCK the delete (the refactoring cannot auto-clean them; a confirm=true delete is refused unless force=true)",
          "type": "boolean"
        },
        "blockingReferences": {
          "description": "Incoming references the refactoring cannot auto-clean: listed in the preview, the reason a delete is refused (action='blocked'), or left dangling when force=true (action='executed')",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "blockingReferencesCount": {
          "description": "Count of blocking references",
          "type": "integer"
        },
        "forced": {
          "description": "Whether the delete was forced past blocking references",
          "type": "boolean"
        },
        "fqn": {
          "description": "FQN of the node targeted for deletion",
          "type": "string"
        },
        "items": {
          "description": "Metadata items the deletion would remove (preview)",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "message": {
          "description": "Human-readable description of the result",
          "type": "string"
        },
        "refactoringTitle": {
          "description": "Title of the delete refactoring (preview)",
          "type": "string"
        },
        "success": {
          "description": "Whether the request succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": true,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Remove an EDT project from the workspace, optionally deleting its files from disk (deleteContent). Destructive: guarded by a confirm-preview - call without confirm to preview what would be removed (no change), then confirm=true to delete. The inverse of import_configuration_from_xml. Full parameters: call get_tool_guide('delete_project').",
    "inputSchema": {
      "properties": {
        "confirm": {
          "description": "true = perform the removal; default false = preview only (what would be removed, no change).",
          "type": "boolean"
        },
        "deleteContent": {
          "description": "true = also delete the project's files from disk; default false = only unregister the project from the workspace (files stay on disk).",
          "type": "boolean"
        },
        "projectName": {
          "description": "Name of the EDT project to remove from the workspace.",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "delete_project",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "Either 'preview' (nothing changed) or 'deleted' (removed).",
          "type": "string"
        },
        "confirmationRequired": {
          "description": "true on a preview (no change made); absent/false once deleted.",
          "type": "boolean"
        },
        "deleteContent": {
          "description": "Whether disk files were (or would be) deleted too.",
          "type": "boolean"
        },
        "message": {
          "description": "Human-readable status message.",
          "type": "string"
        },
        "project": {
          "description": "Name of the targeted project.",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Reveal (or hide) tool groups for progressive disclosure. Pass toolsets=[ids] from list_toolsets to reveal them, then RE-REQUEST tools/list to see the newly revealed tools. Set disable=true to hide. The 'core' toolset is always visible and cannot be toggled. When progressive disclosure is off, all tools are already listed and this has no effect until you enable it in EDT Preferences → MCP Server.",
    "inputSchema": {
      "properties": {
        "disable": {
          "description": "Hide the listed toolsets instead of revealing them (default false).",
          "type": "boolean"
        },
        "toolsets": {
          "description": "Toolset ids to reveal (or hide with disable=true), e.g. [\"code\",\"debug\"]. Call list_toolsets for the valid ids.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "toolsets"
      ],
      "type": "object"
    },
    "name": "enable_toolset",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "'enabled' or 'disabled'",
          "type": "string"
        },
        "applied": {
          "description": "Toolset ids the change was applied to",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ignored": {
          "description": "Requested ids that cannot be toggled (core)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "invalid": {
          "description": "Requested ids that are not valid toolsets",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "note": {
          "description": "Human-readable next step",
          "type": "string"
        },
        "progressiveDisclosure": {
          "description": "Whether progressive disclosure is currently on",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "visibleToolsets": {
          "description": "Toolset ids visible in tools/list after the change",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Evaluate a BSL expression in the context of a suspended stack frame. Pass frameRef from wait_for_break and the expression text. WARNING: this executes arbitrary BSL code in the running 1C application.",
    "inputSchema": {
      "properties": {
        "expression": {
          "description": "BSL expression to evaluate (required)",
          "type": "string"
        },
        "frameRef": {
          "description": "Stable frame reference from wait_for_break (required)",
          "type": "integer"
        }
      },
      "required": [
        "frameRef",
        "expression"
      ],
      "type": "object"
    },
    "name": "evaluate_expression",
    "outputSchema": {
      "properties": {
        "fullLength": {
          "description": "Full length of value before truncation",
          "type": "integer"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "truncated": {
          "description": "True when value was truncated to the max length",
          "type": "boolean"
        },
        "type": {
          "description": "BSL reference type name of the evaluated value",
          "type": "string"
        },
        "value": {
          "description": "String representation of the evaluated value (may be truncated)",
          "type": "string"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Export an EDT configuration project to XML files (EDT menu: Export -> Configuration to XML Files). Equivalent of 1C platform DumpConfigToFiles.",
    "inputSchema": {
      "properties": {
        "outputPath": {
          "description": "Filesystem path of the output directory for the XML files (required)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name to export (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "outputPath"
      ],
      "type": "object"
    },
    "name": "export_configuration_to_xml",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Find every place a metadata object is used: BSL code modules (with line numbers), other metadata, forms, roles, subsystems, etc. Pass the object FQN; the type token may be English or Russian (e.g. 'Catalog.Products' or its Russian spelling). Use this for all USAGES of a symbol; for where it is DEFINED use go_to_definition, for a literal (non-symbol) text scan use search_in_code. Full parameters and examples: call get_tool_guide('find_references').",
    "inputSchema": {
      "properties": {
        "limit": {
          "description": "Result-size hint (default 100, max 500); caps the overall reference count (at limit*10), not per category.",
          "type": "integer"
        },
        "objectFqn": {
          "description": "FQN of the object to search for, e.g. 'Catalog.Products' (type token may be English or Russian) (required)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "objectFqn"
      ],
      "type": "object"
    },
    "name": "find_references",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Generate translation strings (.lstr/.trans/.dict) for a configuration project: scans translatable features and writes the resulting keys into the project's storages (EDT menu Translation -> Generate translation strings). Run on the configuration project (V8ConfigurationNature), not a dictionary storage project; requires LanguageTool installed in EDT. Full parameters and examples: call get_tool_guide('generate_translation_strings').",
    "inputSchema": {
      "properties": {
        "collectInterface": {
          "description": "Generate interface (.lstr) keys. Default: true.",
          "type": "boolean"
        },
        "collectModel": {
          "description": "Generate model (.trans) keys. Default: true.",
          "type": "boolean"
        },
        "collectModelType": {
          "description": "Model mode: ANY | NONE | COMPUTED_ONLY | UNKNOWN_ONLY | TAGS_ONLY. Default: ANY.",
          "type": "string"
        },
        "fillUpType": {
          "description": "Pre-fill source: NOT_FILLUP | FROM_SOURCE_LANGUAGE | FROM_PROVIDER. Default: NOT_FILLUP.",
          "type": "string"
        },
        "projectName": {
          "description": "Configuration project name (V8ConfigurationNature), not a dictionary storage project. Required.",
          "type": "string"
        },
        "providerId": {
          "description": "Translation provider ID; required only when fillUpType=FROM_PROVIDER (see get_translation_project_info).",
          "type": "string"
        },
        "storageId": {
          "description": "Storage ID to write keys into (see get_translation_project_info). Default: \"edit:default\".",
          "type": "string"
        },
        "targetLanguages": {
          "description": "Target language codes to generate, e.g. [\"en\"]. Required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "projectName",
        "targetLanguages"
      ],
      "type": "object"
    },
    "name": "generate_translation_strings",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get list of applications (infobases) for a project. Returns application ID, name, type, and update state. Application ID is required for update_database and debug_launch tools.",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "get_applications",
    "outputSchema": {
      "properties": {
        "applications": {
          "description": "Applications with id, name, type and update state",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "count": {
          "description": "Number of applications found",
          "type": "integer"
        },
        "defaultApplicationId": {
          "description": "Id of the project's default application",
          "type": "string"
        },
        "inheritedFromProject": {
          "description": "Base/parent project the applications are inherited from (present only for external-objects/extension projects whose applications come from their base project).",
          "type": "string"
        },
        "message": {
          "description": "Informational message when no applications are found",
          "type": "string"
        },
        "project": {
          "description": "EDT project name the applications belong to",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get detailed description of an EDT check by its ID. Returns markdown content with check explanation, examples, and how to fix. Accepts the symbolic check id OR the short UID code shown by get_project_errors (pass projectName so the UID can be resolved). Requires a configured check-descriptions folder (MCP preferences); without it the tool returns a configuration error.",
    "inputSchema": {
      "properties": {
        "checkId": {
          "description": "Check id: the symbolic dash-cased id (e.g. 'begin-transaction', 'ql-temp-table-index') OR the short UID code from get_project_errors (e.g. 'SU23'); a UID is resolved when projectName is also supplied. Precondition: a check-descriptions folder must be configured in MCP preferences, else the tool returns a configuration error.",
          "type": "string"
        },
        "projectName": {
          "description": "Optional EDT project name. Required only to resolve a short UID checkId (e.g. 'SU23') to its symbolic id; ignored when checkId is already symbolic.",
          "type": "string"
        }
      },
      "required": [
        "checkId"
      ],
      "type": "object"
    },
    "name": "get_check_description",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get 1C:Enterprise configuration properties (name, synonym, comment, script variant, compatibility mode, etc.)",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "Project name (optional, if not specified returns first configuration project)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_configuration_properties",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get code-completion proposals at a 1-based line/column in a BSL module - the members, globals and variables valid at that caret (e.g. after a '.'). May return a 'not ready' error while the editor loads; just retry. Full parameters and examples: call get_tool_guide('get_content_assist').",
    "inputSchema": {
      "properties": {
        "column": {
          "description": "Column number (1-based)",
          "type": "integer"
        },
        "contains": {
          "description": "Keep proposals whose display string contains any of these substrings (comma-separated, e.g. 'Insert,Add')",
          "type": "string"
        },
        "extendedDocumentation": {
          "description": "Include full documentation per proposal (default: false)",
          "type": "boolean"
        },
        "filePath": {
          "description": "Deprecated alias for modulePath",
          "type": "string"
        },
        "limit": {
          "description": "Max proposals to return (default: from preferences, max 1000)",
          "type": "integer"
        },
        "line": {
          "description": "Line number (1-based)",
          "type": "integer"
        },
        "modulePath": {
          "description": "BSL module path under src (e.g. 'CommonModules/MyModule/Module.bsl'); alias: filePath",
          "type": "string"
        },
        "offset": {
          "description": "Skip first N matching proposals for pagination (default: 0)",
          "type": "integer"
        },
        "projectName": {
          "description": "EDT project name",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "line",
        "column"
      ],
      "type": "object"
    },
    "name": "get_content_assist",
    "outputSchema": {
      "properties": {
        "column": {
          "description": "1-based column where proposals were computed",
          "type": "integer"
        },
        "file": {
          "description": "Workspace-relative path of the module the proposals are for",
          "type": "string"
        },
        "filteredOut": {
          "description": "Proposals removed by the contains filter",
          "type": "integer"
        },
        "line": {
          "description": "1-based line where proposals were computed",
          "type": "integer"
        },
        "proposals": {
          "description": "Proposals as { displayString[, documentation] }",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "returnedProposals": {
          "description": "Count of proposals returned in this page",
          "type": "integer"
        },
        "skipped": {
          "description": "Proposals consumed by the offset",
          "type": "integer"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "totalProposals": {
          "description": "Total proposals offered before any filter",
          "type": "integer"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Returns the running 1C:EDT version as a plain version string. Returns \"Unknown\" when the version cannot be determined.",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "get_edt_version",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Return a YAML snapshot of a form's calculated WYSIWYG layout (bounds, element types, display properties) as text; use it to inspect or compare what a form actually renders. Requires EDT launched with -DnativeFormBufferedLayoutRender=true, else the result is blank (missing flag, not a bad call). Full parameters and examples: call get_tool_guide('get_form_layout_snapshot').",
    "inputSchema": {
      "properties": {
        "formPath": {
          "description": "Form FQN (e.g. 'Catalog.Products.Forms.ItemForm' or 'CommonForm.MyForm'); if omitted, uses the active form editor.",
          "type": "string"
        },
        "mode": {
          "description": "Output mode: 'compact' (default, visible elements only) or 'full' (all nodes/properties)",
          "enum": [
            "compact",
            "full"
          ],
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name. Required when formPath is specified.",
          "type": "string"
        },
        "refresh": {
          "description": "Force WYSIWYG refresh before snapshot (default: true)",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_form_layout_snapshot",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Capture a PNG screenshot of a form's WYSIWYG editor; pass formPath to open the form automatically or omit it to shoot the active editor. Requires EDT launched with -DnativeFormBufferedLayoutRender=true, else the image is blank (missing flag, not a bad call). Full parameters and examples: call get_tool_guide('get_form_screenshot').",
    "inputSchema": {
      "properties": {
        "formPath": {
          "description": "Form FQN (e.g. 'Catalog.Products.Forms.ItemForm' or 'CommonForm.MyForm'); if omitted, captures the active form editor.",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name. Required when formPath is specified.",
          "type": "string"
        },
        "refresh": {
          "description": "Force a real WYSIWYG re-render before capture; fails with an explicit error instead of returning a stale image when the re-render cannot be completed (default: false)",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_form_screenshot",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "List workspace markers: bookmarks and/or task markers (TODO, FIXME, XXX, HACK). Filter by markerKind (bookmark | task; omit to list both), projectName, filePath substring, and - for task markers only - priority. Returns a markdown table of Kind, Type, Priority, Message, Path and Line.",
    "inputSchema": {
      "properties": {
        "filePath": {
          "description": "Filter by file path substring (optional)",
          "type": "string"
        },
        "limit": {
          "description": "Maximum number of results (default: 100, max: 1000)",
          "type": "integer"
        },
        "markerKind": {
          "description": "Which markers to list: 'bookmark' (manual navigation bookmarks) or 'task' (TODO/FIXME/XXX/HACK code markers). Omit to list both.",
          "enum": [
            "bookmark",
            "task"
          ],
          "type": "string"
        },
        "priority": {
          "description": "Filter task markers by priority (optional). Applies to task markers only; bookmarks have no priority and are unaffected. Cannot be combined with markerKind=bookmark.",
          "enum": [
            "high",
            "normal",
            "low"
          ],
          "type": "string"
        },
        "projectName": {
          "description": "Filter by project name (optional)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_markers",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get detailed properties of one or more 1C metadata objects (basic info by default, or every reflected section with 'full: true'). Use it after get_metadata_objects to inspect a known object's attributes/forms/commands; in full mode each section is capped so request fewer FQNs to keep the response small. A FORM FQN ('Catalog.X.Form.ItemForm' or 'CommonForm.Name') renders that form's STRUCTURE (items / attributes / commands). Use this for the full properties of one named object; to list objects by type use get_metadata_objects. Full parameters and examples: call get_tool_guide('get_metadata_details').",
    "inputSchema": {
      "properties": {
        "assignable": {
          "description": "Instead of the details view, return the ASSIGNABLE-property schema (default false): per property its value kind, current value and ALLOWED values (enum literals). This is what modify_metadata can set; FQNs may address members (e.g. 'Catalog.Products.Attribute.Weight').",
          "type": "boolean"
        },
        "full": {
          "description": "All reflected properties (true) or only key info (false). Default: false",
          "type": "boolean"
        },
        "language": {
          "description": "Synonym language code, e.g. 'en'/'ru' (default: configuration default)",
          "type": "string"
        },
        "objectFqns": {
          "description": "Required. FQNs as Type.Name, e.g. ['Catalog.Products', 'Document.SalesOrder']; Russian type tokens also work (e.g. 'Справочник.Products').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "objectFqns"
      ],
      "type": "object"
    },
    "name": "get_metadata_details",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get a flat list of 1C configuration metadata objects (Name, Synonym, Comment, Type, ObjectModule, ManagerModule) as a Markdown table. Use it to discover what objects exist; filter by metadataType and/or nameFilter (Name only). Use this to list objects; for the full properties of one named object use get_metadata_details. Full parameters and examples: call get_tool_guide('get_metadata_objects').",
    "inputSchema": {
      "properties": {
        "language": {
          "description": "Synonym language code, e.g. 'en'/'ru' (default: configuration default)",
          "type": "string"
        },
        "limit": {
          "description": "Max rows (default from preferences: 100, max 1000)",
          "type": "integer"
        },
        "metadataType": {
          "description": "Type filter (case-insensitive), default 'all'. One of: all, documents, catalogs, informationRegisters, accumulationRegisters, commonModules, enums, constants, reports, dataProcessors, exchangePlans, businessProcesses, tasks, commonAttributes, eventSubscriptions, scheduledJobs",
          "type": "string"
        },
        "nameFilter": {
          "description": "Case-insensitive substring matched against Name only (not Synonym)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "get_metadata_objects",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Find a BSL method's call hierarchy: who calls it (callers, default) or what it calls (callees), via semantic AST analysis that resolves ru/en spellings (unlike literal search_in_code). Full parameters and examples: call get_tool_guide('get_method_call_hierarchy').",
    "inputSchema": {
      "properties": {
        "direction": {
          "description": "'callers' (default) or 'callees'",
          "enum": [
            "callers",
            "callees"
          ],
          "type": "string"
        },
        "limit": {
          "description": "Max results. Default: 100, max: 500",
          "type": "integer"
        },
        "methodName": {
          "description": "Name of the procedure/function (case-insensitive, required)",
          "type": "string"
        },
        "modulePath": {
          "description": "Path from src/ folder, e.g. 'CommonModules/MyModule/Module.bsl' (required)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "modulePath",
        "methodName"
      ],
      "type": "object"
    },
    "name": "get_method_call_hierarchy",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get structure of a BSL module: all procedures/functions with signatures, line numbers, regions, execution context (&AtServer, &AtClient), export flag, and parameters. responseFormat=concise (default) returns a leaner methods table (drops the verbose Parameters and Description columns; keeps type, name, export, context, lines, region); responseFormat=detailed returns the full table with signatures and doc-comments. Use detailed when you need parameter lists or descriptions. Use this for the structure of ONE module; to discover module paths across a project use list_modules.",
    "inputSchema": {
      "properties": {
        "includeComments": {
          "description": "Include documentation comments for methods. Default: false",
          "type": "boolean"
        },
        "includeVariables": {
          "description": "Include module-level variable declarations. Default: false",
          "type": "boolean"
        },
        "modulePath": {
          "description": "Path from src/ folder, e.g. 'CommonModules/MyModule/Module.bsl' (required)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        },
        "responseFormat": {
          "description": "Output verbosity. concise (default) = leaner methods table (drops Parameters and Description columns); detailed = full table with signatures and doc-comments.",
          "enum": [
            "concise",
            "detailed"
          ],
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "modulePath"
      ],
      "type": "object"
    },
    "name": "get_module_structure",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get metadata objects filtered by tags. Returns objects that have any of the specified tags, including tag descriptions and object FQNs.",
    "inputSchema": {
      "properties": {
        "limit": {
          "description": "Maximum number of objects to return per tag. Default: 100",
          "type": "integer"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        },
        "tags": {
          "description": "Array of tag names to filter by (e.g. ['Important', 'NeedsReview']). Returns objects that have ANY of these tags. Required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "projectName",
        "tags"
      ],
      "type": "object"
    },
    "name": "get_objects_by_tags",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Look up 1C:Enterprise platform documentation for built-in types (ValueTable, Array, Structure) and global built-in functions, including their methods, properties, constructors and events. Use when you need the exact platform API signature rather than configuration metadata. Full parameters and examples: call get_tool_guide('get_platform_documentation').",
    "inputSchema": {
      "properties": {
        "category": {
          "description": "'type' (platform types) or 'builtin' (global functions). Default: 'type'.",
          "enum": [
            "type",
            "builtin"
          ],
          "type": "string"
        },
        "language": {
          "description": "Output language. Default: 'en'.",
          "enum": [
            "en",
            "ru"
          ],
          "type": "string"
        },
        "limit": {
          "description": "Max results. Default: 50, max 200.",
          "type": "integer"
        },
        "memberName": {
          "description": "Filter members by name, partial match (e.g. 'Add').",
          "type": "string"
        },
        "memberType": {
          "description": "Filter by member kind. Default: 'all'.",
          "enum": [
            "method",
            "property",
            "constructor",
            "event",
            "all"
          ],
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name to pin the platform version. Optional.",
          "type": "string"
        },
        "responseFormat": {
          "description": "'concise' (default) = leaner: headers + member names only; 'detailed' = full member signatures, parameters, types and flags.",
          "enum": [
            "concise",
            "detailed"
          ],
          "type": "string"
        },
        "typeName": {
          "description": "Type or symbol name, English or Russian (e.g. 'ValueTable').",
          "type": "string"
        }
      },
      "required": [
        "typeName"
      ],
      "type": "object"
    },
    "name": "get_platform_documentation",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get problem summary with counts grouped by project and EDT severity level (ERRORS, BLOCKER, CRITICAL, MAJOR, MINOR, TRIVIAL). Use this for severity totals only; for the detailed per-marker list call get_project_errors.",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "Name of the project (optional, all projects if not specified)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_problem_summary",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get profiling (performance measurement) results after a debug session: per-module, per-line call count, timing and percentage. Returns only the MOST RECENT measurement session (historical sessions are not returned). Also reports whether profiling is currently active. Run start_profiling + the test (then stop_profiling) first. Full parameters and examples: call get_tool_guide('get_profiling_results').",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Debug session id; when set, reports active state for that session",
          "type": "string"
        },
        "minFrequency": {
          "description": "Only include lines called at least N times (default: 1)",
          "type": "integer"
        },
        "moduleFilter": {
          "description": "Case-insensitive substring filter on module name",
          "type": "string"
        },
        "responseFormat": {
          "description": "concise (default) = leaner per-line rows (line, calls, pct only); detailed = full rows incl. code text, method signature and dur/pureDur timing",
          "enum": [
            "concise",
            "detailed"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_profiling_results",
    "outputSchema": {
      "properties": {
        "count": {
          "description": "Number of result sets returned (0 or 1 — only the latest session)",
          "type": "integer"
        },
        "message": {
          "description": "Informational note when no results are available",
          "type": "string"
        },
        "profilingActive": {
          "description": "Whether profiling is currently active",
          "type": "boolean"
        },
        "results": {
          "description": "Per-result sets: name, totalDurability, moduleCount and modules map of lines",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "List EDT configuration problems (validation markers) with optional project / severity / check-id / object filters. Each row carries the check code, message, object location and severity; BSL-module problems also expose a structural locator (Module path + Line) you can feed straight into read_module_source or set_breakpoint. Object FQN filters accept English or Russian type names (e.g. 'Catalog.Products'). Use this for the detailed marker list; for severity totals only call get_problem_summary. Full parameters and examples: call get_tool_guide('get_project_errors').",
    "inputSchema": {
      "properties": {
        "checkId": {
          "description": "Filter by check-id substring; matches the symbolic id (e.g. 'ql-temp-table-index') or short UID (e.g. 'SU23') (optional)",
          "type": "string"
        },
        "limit": {
          "description": "Max results; default 100, max 1000 (optional)",
          "type": "integer"
        },
        "objects": {
          "description": "Filter by object FQNs, e.g. ['Catalog.Products']; English or Russian type names accepted (optional)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "projectName": {
          "description": "Filter by EDT project name; omit to scan all projects (optional)",
          "type": "string"
        },
        "responseFormat": {
          "description": "Output verbosity (optional): concise (default) = leaner table without the secondary 'Has docs' column; detailed = full table including 'Has docs'",
          "enum": [
            "concise",
            "detailed"
          ],
          "type": "string"
        },
        "severity": {
          "description": "Filter by severity (optional)",
          "enum": [
            "ERRORS",
            "BLOCKER",
            "CRITICAL",
            "MAJOR",
            "MINOR",
            "TRIVIAL",
            "NONE"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_project_errors",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Self-diagnosis snapshot of the running MCP server: listening port, MCP protocol version, plugin version, EDT version, enabled/total tool counts, the plainTextMode and checksFolderConfigured preference flags, the two form-render JVM flags (nativeFormBufferedLayoutRender / nativeFormLayoutRender), and whether authentication is enabled. Use it to explain a blank form screenshot or a plain-text JSON response. Never returns the auth token value or the checks folder path, only booleans.",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "get_server_status",
    "outputSchema": {
      "properties": {
        "authEnabled": {
          "description": "Whether bearer-token authentication is enabled",
          "type": "boolean"
        },
        "checksFolderConfigured": {
          "description": "Whether a checks folder path is configured",
          "type": "boolean"
        },
        "edtVersion": {
          "description": "Detected 1C:EDT version",
          "type": "string"
        },
        "enabledTools": {
          "description": "Number of tools currently enabled",
          "type": "integer"
        },
        "formRenderFlags": {
          "description": "Form-render JVM flag states keyed by flag name",
          "type": "object"
        },
        "plainTextMode": {
          "description": "Whether JSON responses are forced to plain text",
          "type": "boolean"
        },
        "pluginVersion": {
          "description": "Version of the EDT-MCP plugin",
          "type": "string"
        },
        "port": {
          "description": "TCP port the MCP server listens on",
          "type": "integer"
        },
        "protocolVersion": {
          "description": "MCP protocol version implemented by the server",
          "type": "string"
        },
        "running": {
          "description": "Whether the MCP server is currently running",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "totalTools": {
          "description": "Total number of registered tools",
          "type": "integer"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get one 1C subsystem's content: properties, its metadata objects (Type/Name/Synonym/FQN) and child subsystems, identified by FQN (e.g. 'Subsystem.Sales.Subsystem.Orders'). By default lists only this subsystem's objects; set recursive=true to fold in nested ones. Full parameters and examples: call get_tool_guide('get_subsystem_content').",
    "inputSchema": {
      "properties": {
        "language": {
          "description": "Synonym language code, e.g. 'en'/'ru' (default: configuration default)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        },
        "recursive": {
          "description": "Also include objects from nested subsystems (default: false)",
          "type": "boolean"
        },
        "subsystemFqn": {
          "description": "Subsystem FQN (required), e.g. 'Subsystem.Sales.Subsystem.Orders'",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "subsystemFqn"
      ],
      "type": "object"
    },
    "name": "get_subsystem_content",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get type/hover info about a symbol at a position in a BSL module. Returns inferred types, signatures, and documentation.",
    "inputSchema": {
      "properties": {
        "column": {
          "description": "Column number (1-based)",
          "type": "integer"
        },
        "filePath": {
          "description": "Deprecated alias for modulePath",
          "type": "string"
        },
        "line": {
          "description": "Line number (1-based)",
          "type": "integer"
        },
        "modulePath": {
          "description": "BSL module path from src/, e.g. 'CommonModules/MyModule/Module.bsl' (canonical; alias: filePath)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "line",
        "column"
      ],
      "type": "object"
    },
    "name": "get_symbol_info",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get list of all tags defined in the project. Tags are user-defined labels for organizing metadata objects. Returns tag name, color, description, and number of assigned objects.",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "get_tags",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Capture a PNG screenshot of a 1C template (a SpreadsheetDocument print form) as EDT renders it, so its layout and text are visible to an AI. Works for a common template 'CommonTemplate.<Name>' OR an object-owned template '<Type>.<Owner>.Template.<Name>' (e.g. 'DataProcessor.Invoices.Template.Printout'). Renders off-screen (no JVM flag needed). Full parameters and examples: call get_tool_guide('get_template_screenshot').",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "EDT project name.",
          "type": "string"
        },
        "templatePath": {
          "description": "Template FQN: a common template 'CommonTemplate.<Name>' or an object-owned template '<Type>.<Owner>.Template.<Name>' (e.g. 'DataProcessor.Invoices.Template.Printout').",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "templatePath"
      ],
      "type": "object"
    },
    "name": "get_template_screenshot",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Get the full on-demand how-to for a tool: its description, every parameter (type, required, allowed values) and extended examples/preconditions kept OUT of the always-loaded tool list to save context. Pass the exact tool name from tools/list. Also available as the resource guide://<toolName>.",
    "inputSchema": {
      "properties": {
        "toolName": {
          "description": "The exact name of the tool to document, as it appears in tools/list (e.g. 'read_module_source').",
          "type": "string"
        }
      },
      "required": [
        "toolName"
      ],
      "type": "object"
    },
    "name": "get_tool_guide",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Return LanguageTool metadata for a project: the translation storages declared on it and the available translation provider IDs. Use it to check whether a dictionary storage is attached before translating; an empty storages list means none is attached yet (set up manually in EDT). Requires EDT with LanguageTool installed. Full parameters and examples: call get_tool_guide('get_translation_project_info').",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "get_translation_project_info",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Read variables from a stack frame of a suspended debug thread. Pass frameRef from wait_for_break (preferred) or threadId+frameIndex. Use expandPath to drill into nested structures (dot-separated).",
    "inputSchema": {
      "properties": {
        "expandPath": {
          "description": "Dot-separated path to expand a nested variable",
          "type": "string"
        },
        "frameIndex": {
          "description": "0-based frame index when using threadId",
          "type": "integer"
        },
        "frameRef": {
          "description": "Stable frame reference returned from wait_for_break",
          "type": "integer"
        },
        "threadId": {
          "description": "Thread id (alternative to frameRef)",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "get_variables",
    "outputSchema": {
      "properties": {
        "count": {
          "description": "Number of variables returned",
          "type": "integer"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "variables": {
          "description": "Variables in the frame or expanded node, with name/value/type",
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Go to the definition of a symbol (the inverse of find_references): a qualified method 'ModuleName.MethodName', a bare 'MethodName' (also pass modulePath), or a metadata FQN like 'Catalog.Products'. A bare method name requires modulePath. Use this for where a symbol is DEFINED; for all its USAGES use find_references, for a literal (non-symbol) text scan use search_in_code. Full parameters and examples: call get_tool_guide('go_to_definition').",
    "inputSchema": {
      "properties": {
        "includeSource": {
          "description": "Include the source body (default true)",
          "type": "boolean"
        },
        "modulePath": {
          "description": "Module path from src/, e.g. 'CommonModules/My/Module.bsl'; required for a bare method name",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name",
          "type": "string"
        },
        "symbol": {
          "description": "'ModuleName.MethodName', bare 'MethodName' (needs modulePath), or metadata FQN 'Catalog.Products'",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "symbol"
      ],
      "type": "object"
    },
    "name": "go_to_definition",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Import a configuration from a directory of XML files into a NEW EDT project (EDT menu: Import); the reverse of export_configuration_to_xml. The projectName must not already exist in the workspace. Full parameters and examples: call get_tool_guide('import_configuration_from_xml').",
    "inputSchema": {
      "properties": {
        "importPath": {
          "description": "Path of the source directory of XML files.",
          "type": "string"
        },
        "projectName": {
          "description": "Name of the NEW EDT project to create (must not already exist).",
          "type": "string"
        },
        "projectNature": {
          "description": "Optional EDT nature ID, e.g. 'com._1c.g5.v8.dt.core.V8ConfigurationNature'; empty = auto-detect.",
          "type": "string"
        },
        "xmlVersion": {
          "description": "Optional XML format version, e.g. '8.3.20'; empty = auto-detect.",
          "type": "string"
        }
      },
      "required": [
        "importPath",
        "projectName"
      ],
      "type": "object"
    },
    "name": "import_configuration_from_xml",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "List active line breakpoints. Optionally filter by projectName.",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "Optional project filter",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "list_breakpoints",
    "outputSchema": {
      "properties": {
        "breakpoints": {
          "description": "List of active line breakpoints",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "count": {
          "description": "Number of breakpoints returned",
          "type": "integer"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "List EDT launch configurations (runtime client + Attach + other 1C types) with their running state. This is the discovery step before debug_launch / run_yaxunit_tests / debug_yaxunit_tests / update_database: use the returned 'name' as their launchConfigurationName. Use type='attach' for server-side debug setups, type='client' for 1C:Enterprise client configs, or type='all' (default). Full parameters and examples: call get_tool_guide('list_configurations').",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "Optional exact project-name filter.",
          "type": "string"
        },
        "type": {
          "description": "Filter by config kind; default 'all'. e.g. 'attach' for server-side debug.",
          "enum": [
            "attach",
            "client",
            "all"
          ],
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "list_configurations",
    "outputSchema": {
      "properties": {
        "configurations": {
          "description": "Matching launch configurations with their state.",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "count": {
          "description": "Number of configurations returned.",
          "type": "integer"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "List BSL modules in an EDT project as a table (module path, module type, parent type, parent name). Use it to discover module paths before reading or editing code; filter by metadataType, objectName or nameFilter. Use this to enumerate a project's modules; for the methods/regions inside one module use get_module_structure. Full parameters and examples: call get_tool_guide('list_modules').",
    "inputSchema": {
      "properties": {
        "limit": {
          "description": "Max rows, default 200 (clamped to 1000)",
          "type": "integer"
        },
        "metadataType": {
          "description": "Type filter, default 'all' (case-insensitive).",
          "enum": [
            "all",
            "documents",
            "catalogs",
            "commonModules",
            "informationRegisters",
            "accumulationRegisters",
            "reports",
            "dataProcessors",
            "exchangePlans",
            "businessProcesses",
            "tasks",
            "constants",
            "commonCommands",
            "commonForms",
            "webServices",
            "httpServices"
          ],
          "type": "string"
        },
        "nameFilter": {
          "description": "Case-insensitive substring matched against the module path",
          "type": "string"
        },
        "objectName": {
          "description": "Programmatic Name of one object to scope to, e.g. 'Products' (case-insensitive)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "list_modules",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "List all workspace projects with properties (name, path, type, natures)",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "list_projects",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "List 1C subsystems of a configuration as a flat table (FQN, Synonym, Comment, InCommandInterface, content count, children count). Walks the whole tree by default (recursive=true); use get_subsystem_content for one subsystem's objects. Full parameters and examples: call get_tool_guide('list_subsystems').",
    "inputSchema": {
      "properties": {
        "language": {
          "description": "Synonym language code, e.g. 'en'/'ru' (default: configuration default)",
          "type": "string"
        },
        "limit": {
          "description": "Max results (default from preferences: 100, max 1000)",
          "type": "integer"
        },
        "nameFilter": {
          "description": "Case-insensitive partial match on Name only (not Synonym)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        },
        "recursive": {
          "description": "Include nested subsystems (default: true)",
          "type": "boolean"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "list_subsystems",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "List the tool groups (toolsets) used by progressive tool disclosure: each toolset's id, title, description, member tools, and whether it is currently visible in tools/list. When progressive disclosure is on, only the 'core' toolset plus toolsets you reveal with enable_toolset appear in tools/list; when off (default) all tools are already listed. Read-only. Use it to discover which toolset id to pass to enable_toolset.",
    "inputSchema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "list_toolsets",
    "outputSchema": {
      "properties": {
        "count": {
          "description": "Number of toolsets",
          "type": "integer"
        },
        "note": {
          "description": "Human-readable guidance for the current disclosure mode",
          "type": "string"
        },
        "progressiveDisclosure": {
          "description": "Whether progressive disclosure is currently on",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "toolsets": {
          "description": "The toolsets (id, title, description, tools, toolCount, visible, core)",
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Set properties of a metadata node (object or member, including a FORM member - item / attribute / command) addressed by a 1C full-name FQN, as properties=[{name, value, language?}]. Each property is validated (it must be assignable, and an enum value must be one of the allowed literals) with an actionable error. Move/reorder a FORM ITEM with the 'parent' (a group name, 'AutoCommandBar' for the form's command bar, or the form name for the form root) and/or 'position' ('first'/'last'/'before:<name>'/'after:<name>'/index) properties. REBIND a form event handler's procedure with a 'procedure' property on a Handler FQN, or re-point a Button at a different form command with a 'command' property. Set a StyleItem's value with a 'value' property: a Color {value:{color:{red:255,green:0,blue:0}}} (or {color:'auto'}) or a Font {value:{font:{faceName:'Arial',height:12,bold:true}}}. Give a form list FORM ATTRIBUTE a custom dynamic-list query with a 'queryText' property (and 'customQuery' true/false, plus an optional 'mainTable' object FQN): this turns the attribute into a DynamicList and lets EDT auto-fill the available fields from the query (no manual XML; output a column with create_metadata Field dataPath 'List.<field>'). Discover assignable properties + allowed values with get_metadata_details(assignable:true). To rename, use rename_metadata_object. Full parameters and examples: call get_tool_guide('modify_metadata').",
    "inputSchema": {
      "properties": {
        "fqn": {
          "description": "Full-name FQN of the node to modify (required), e.g. 'Catalog.Products' or 'Catalog.Products.Attribute.Weight' (type / kind tokens may be English or Russian; the Name parts are the programmatic Name).",
          "type": "string"
        },
        "normalizeYo": {
          "description": "Normalize the Russian letter 'ё'->'е' / 'Ё'->'Е' in localized-string values (synonym / title) and in the 'comment' property (default true). Matches the 1C standard mdo-ru-name-unallowed-letter. Other free-text strings can be identifier-like (e.g. XDTOPackage.namespace is a URI) and always keep the supplied value. Set false to keep 'ё' exactly as supplied everywhere.",
          "type": "boolean"
        },
        "projectName": {
          "description": "EDT project name (required).",
          "type": "string"
        },
        "properties": {
          "description": "Properties to set, as [{name, value, language?}] (required, at least one). 'name' is the property name (e.g. 'comment', 'synonym', 'indexing'); 'value' is the new value; 'language' is the code for a synonym (default: config default).",
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "projectName",
        "fqn",
        "properties"
      ],
      "type": "object"
    },
    "name": "modify_metadata",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "'modified' on success",
          "type": "string"
        },
        "applied": {
          "description": "Names of the properties that were set",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "destination": {
          "description": "Where a moved form item ended up (when 'parent'/'position' moved a form item), e.g. \"group 'Main' at index 1\"",
          "type": "string"
        },
        "fqn": {
          "description": "Normalized FQN of the modified node",
          "type": "string"
        },
        "message": {
          "description": "Human-readable confirmation message",
          "type": "string"
        },
        "normalized": {
          "description": "Properties whose value was rewritten by the 'ё'->'е' normalization (when any)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "persisted": {
          "description": "Whether the change was exported to disk",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the properties were set",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Read a specific procedure/function from a BSL module by name. Returns source code with metadata. Lists available methods if not found. Use this for one method body; to read the whole module source use read_module_source.",
    "inputSchema": {
      "properties": {
        "methodName": {
          "description": "Procedure/function name (case-insensitive)",
          "type": "string"
        },
        "modulePath": {
          "description": "Path from src/, e.g. 'CommonModules/MyModule/Module.bsl'",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "modulePath",
        "methodName"
      ],
      "type": "object"
    },
    "name": "read_method_source",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Read BSL module source code from an EDT project, whole file or a line range. Returns YAML frontmatter (including a contentHash revision token to round-trip into write_module_source's expectedHash) followed by clean source in a fenced bsl block. Use this for the whole module; to read just one procedure/function body use read_method_source. Full parameters and examples: call get_tool_guide('read_module_source').",
    "inputSchema": {
      "properties": {
        "endLine": {
          "description": "Last line, 1-based inclusive; omit to read to the end.",
          "type": "integer"
        },
        "modulePath": {
          "description": "Path from src/, e.g. 'CommonModules/MyModule/Module.bsl' (required)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        },
        "startLine": {
          "description": "First line, 1-based inclusive; omit to read from the start.",
          "type": "integer"
        }
      },
      "required": [
        "projectName",
        "modulePath"
      ],
      "type": "object"
    },
    "name": "read_module_source",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Remove a 1C BSL line breakpoint. Either pass breakpointId (returned from set_breakpoint) or projectName+module+lineNumber to look it up by coordinates.",
    "inputSchema": {
      "properties": {
        "breakpointId": {
          "description": "Marker id returned by set_breakpoint",
          "type": "integer"
        },
        "lineNumber": {
          "description": "1-based line number (when looking up by coordinates)",
          "type": "integer"
        },
        "module": {
          "description": "Legacy alias for modulePath (deprecated)",
          "type": "string"
        },
        "modulePath": {
          "description": "EDT module path or absolute path (when looking up by coordinates)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (when looking up by coordinates)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "remove_breakpoint",
    "outputSchema": {
      "properties": {
        "removed": {
          "description": "True if a matching breakpoint was found and removed",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": true,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Rename a metadata object or attribute, cascading the change across all references in BSL code, forms, and other metadata. Use the two-phase workflow: call without confirm for an indexed preview of every change point, review it, then call again with confirm=true to apply. Full parameters and examples: call get_tool_guide('rename_metadata_object').",
    "inputSchema": {
      "properties": {
        "confirm": {
          "description": "true = apply the rename; default false = preview only.",
          "type": "boolean"
        },
        "disableIndices": {
          "description": "Comma-separated preview '#' indices of OPTIONAL change points to skip, e.g. '2,3,5'.",
          "type": "string"
        },
        "maxResults": {
          "description": "Max change points shown in the preview (default 20; 0 = no limit).",
          "type": "integer"
        },
        "newName": {
          "description": "New programmatic Name for the object.",
          "type": "string"
        },
        "objectFqn": {
          "description": "FQN of the object to rename, e.g. 'Catalog.Products' or 'Document.SalesOrder.Attribute.Amount' (Russian type names also accepted).",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name.",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "objectFqn",
        "newName"
      ],
      "type": "object"
    },
    "name": "rename_metadata_object",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Resume a suspended debug thread or all threads of a debug target. Pass threadId (from wait_for_break) or applicationId. applicationId accepts ANY id form for the session: the real id, 'attach:<name>', 'launch:<name>', or 'ServerApplication.<app>'. For a server-side suspend, resume targets the suspended thread directly. With no arguments, resumes the single active debug session (launch or server target) if exactly one exists. NOTE: if resume of a server-side suspend does not take effect, the breakpoint can also be released from the EDT UI.",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application id (real, 'attach:<configName>', 'launch:<configName>', or 'ServerApplication.<app>'). Resumes this session's target/suspended thread. Optional if exactly one debug session is active.",
          "type": "string"
        },
        "threadId": {
          "description": "Thread id from wait_for_break",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "resume",
    "outputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application id of the resumed debug target",
          "type": "string"
        },
        "autoResolved": {
          "description": "Whether the lone active session was auto-resolved",
          "type": "boolean"
        },
        "resumed": {
          "description": "Whether the thread or target was resumed",
          "type": "boolean"
        },
        "scope": {
          "description": "Resume scope: 'thread' or 'target'",
          "type": "string"
        },
        "serverTarget": {
          "description": "True if resolved via the 1C debug-server target bridge",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Bulk re-synchronize the in-memory BM model to the on-disk src/ .mdo files and report BM-to-disk desync. Walks EVERY top metadata object of the configuration (all kinds), reports the objects whose .mdo is missing on disk (missingBefore), and force-exports that missing subset so the files are restored (fullExport=true re-exports every object instead). Fixes 'object file does not exist' failures from update_database / XML import caused by an accumulated desync. Dangling/orphaned references in Configuration.mdo (unresolved proxies shown by get_project_errors as md-reference-intergrity 'lost reference' warnings that block update_database / XML import) are REPORTED by default (danglingFound + danglingDetails); set cleanDanglingReferences=true to REMOVE them - destructive: rewrites Configuration.mdo. Full parameters and examples: call get_tool_guide('resync_to_disk').",
    "inputSchema": {
      "properties": {
        "cleanDanglingReferences": {
          "description": "When true, REMOVE dangling/orphaned references from Configuration.mdo - entries that register an object with no .mdo and no BM body (unresolved proxies), the source of md-reference-intergrity 'lost reference' warnings that block update_database / XML import. Destructive: rewrites Configuration.mdo. Default: false - only report danglingFound + danglingDetails without changing anything.",
          "type": "boolean"
        },
        "fullExport": {
          "description": "When true, force-export EVERY metadata top object's .mdo (a full disk refresh; slow on a large configuration - the export runs on the UI thread). Default: false - export only the objects whose .mdo is missing on disk.",
          "type": "boolean"
        },
        "projectName": {
          "description": "EDT project name (required).",
          "type": "string"
        },
        "revalidate": {
          "description": "When true, schedule a full project revalidation (clean build) after the export so stale markers refresh. Default: false (export only).",
          "type": "boolean"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "resync_to_disk",
    "outputSchema": {
      "properties": {
        "cleanDanglingReferences": {
          "description": "Whether dangling-reference removal was requested",
          "type": "boolean"
        },
        "danglingDetails": {
          "description": "All dangling entries found: [{field, lostFqn, position}]",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "danglingFound": {
          "description": "Dangling (unresolved-proxy) references detected in Configuration.mdo",
          "type": "integer"
        },
        "danglingRemoved": {
          "description": "Removed dangling entries: [{field, lostFqn, position}]",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "danglingRemovedCount": {
          "description": "Dangling references actually removed (0 in report-only mode)",
          "type": "integer"
        },
        "danglingWarning": {
          "description": "Set when the dangling scan/cleanup could not complete cleanly",
          "type": "string"
        },
        "fullExport": {
          "description": "Whether a full export of every top object was requested",
          "type": "boolean"
        },
        "message": {
          "description": "Human-readable summary of the outcome",
          "type": "string"
        },
        "missingBefore": {
          "description": "FQNs that were missing on disk before the export",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "missingBeforeCount": {
          "description": "Top objects that had no .mdo on disk before the export",
          "type": "integer"
        },
        "objectsExported": {
          "description": "Number of top objects whose .mdo was (re)written - the missing subset by default, every object when fullExport=true",
          "type": "integer"
        },
        "projectName": {
          "description": "The project that was re-synchronized",
          "type": "string"
        },
        "revalidate": {
          "description": "Whether a post-export revalidation was requested",
          "type": "boolean"
        },
        "revalidateWarning": {
          "description": "Set when the optional post-export revalidation failed",
          "type": "string"
        },
        "stillMissing": {
          "description": "FQNs still missing on disk after the export",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "stillMissingCount": {
          "description": "Top objects still missing on disk after the export",
          "type": "integer"
        },
        "success": {
          "description": "Whether the export succeeded",
          "type": "boolean"
        },
        "totalTopObjects": {
          "description": "Total metadata top objects walked in the BM model",
          "type": "integer"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Revalidate EDT project or specific objects. If objects array is empty or missing, revalidates entire project. FQN examples: 'Document.SalesOrder', 'Catalog.Products', 'CommonModule.Common'. Russian type names are also supported (e.g. 'Документ.ПриходнаяНакладная', 'Справочник.Номенклатура').",
    "inputSchema": {
      "properties": {
        "objects": {
          "description": "FQNs to revalidate (e.g. ['Document.SalesOrder']). Russian type names supported (e.g. 'Документ.ПродажаТоваров'). Empty array = full project revalidation",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        }
      },
      "required": [
        "projectName"
      ],
      "type": "object"
    },
    "name": "revalidate_objects",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Run YAXUnit tests for a 1C:Enterprise project and return a JUnit Markdown report. Polls for up to `timeout` seconds, then returns the report or **Pending** (call again with identical arguments to keep waiting; the launch is not terminated). Pass `debug=true` to instead launch in DEBUG mode (breakpoints fire) and return at once so you can call wait_for_break. The pre-launch auto-chain (updateBeforeLaunch=true, default) recomputes only projects that changed since the last prepared run; the first call after EDT starts always recomputes fully. If recompute+update exceeds 25s the tool returns **Pending** immediately — call again with the same arguments; preparation continues in the background. Requires an existing runtime-client launch configuration and the YAXUnit extension installed in the infobase. Full parameters and examples: call get_tool_guide('run_yaxunit_tests').",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application ID from get_applications (required if launchConfigurationName is omitted).",
          "type": "string"
        },
        "debug": {
          "description": "Default false: poll and return the report. true: launch in DEBUG mode so breakpoints fire, return immediately and call wait_for_break next (ignores timeout).",
          "type": "boolean"
        },
        "extensions": {
          "description": "Extension names to filter tests (array; a comma-separated string is also accepted).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "launchConfigurationName": {
          "description": "Exact runtime-client launch config name (preferred; from list_configurations).",
          "type": "string"
        },
        "modules": {
          "description": "Module names to filter tests (array; a comma-separated string is also accepted).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "projectName": {
          "description": "EDT project name (required if launchConfigurationName is omitted).",
          "type": "string"
        },
        "tests": {
          "description": "Test names in Module.Method format (array; a comma-separated string is also accepted).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "description": "Polling window in seconds (default: 60); on expiry returns Pending.",
          "type": "integer"
        },
        "updateBeforeLaunch": {
          "description": "Auto-chain (default: true): force-recompute the project + its extensions, terminate a live client and run a silent DB update first, so a freshly edited extension runs fresh (not stale). false: legacy delegate behaviour — no client sweep, no auto-confirmed update dialog; platform dialogs may appear and block. Results are never served from a cache — a completed run is re-executed on the next identical call regardless of this flag.",
          "type": "boolean"
        },
        "updateScope": {
          "description": "Which projects to rebuild+update before the run: 'all' (configuration + dependent extensions, default), 'configuration', or 'extension:<ProjectName>' (comma-separate several). Forces a derived-data recompute so a freshly edited extension's .cfe is regenerated and loaded into the infobase before the run. Unknown extension names fail the call (the error lists the available names). Only applies when updateBeforeLaunch=true.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "run_yaxunit_tests",
    "outputSchema": null
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Literal/regex full-text search across all BSL modules in a project. Matching is purely textual and NOT ru/en dialect-aware, so a query in one BSL language won't find the other spelling; for identifier lookup use get_symbol_info, find_references or get_method_call_hierarchy instead. Use this for a literal text scan; for a symbol's USAGES use find_references, for where it is DEFINED use go_to_definition. Full parameters and examples: call get_tool_guide('search_in_code').",
    "inputSchema": {
      "properties": {
        "caseSensitive": {
          "description": "Case-sensitive search. Default: false",
          "type": "boolean"
        },
        "contextLines": {
          "description": "Lines of context before/after each match. Default: 2, max: 5",
          "type": "integer"
        },
        "fileMask": {
          "description": "Filter by module path substring (e.g. 'CommonModules' or 'Documents/SalesOrder')",
          "type": "string"
        },
        "isRegex": {
          "description": "Treat query as a regular expression. Default: false",
          "type": "boolean"
        },
        "limit": {
          "description": "Max matches returned with context. Default: 100, max: 500",
          "type": "integer"
        },
        "maxResults": {
          "description": "Deprecated alias for 'limit'. Default: 100, max: 500",
          "type": "integer"
        },
        "metadataType": {
          "description": "Filter by metadata type (e.g. 'documents', 'catalogs', 'commonModules'); more precise than fileMask. See guide for the full list.",
          "type": "string"
        },
        "outputMode": {
          "description": "Output mode: 'full' (matches with context, default), 'count', or 'files'",
          "enum": [
            "full",
            "count",
            "files"
          ],
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        },
        "query": {
          "description": "Search string or regex pattern (required); matched literally unless isRegex=true",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "query"
      ],
      "type": "object"
    },
    "name": "search_in_code",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Set a line breakpoint on a 1C BSL module. Accepts either an EDT module-relative path (e.g. 'CommonModules/Foo/Module.bsl') or an absolute filesystem path. Use wait_for_break afterwards to block until the breakpoint is hit.",
    "inputSchema": {
      "properties": {
        "lineNumber": {
          "description": "1-based line number (required)",
          "type": "integer"
        },
        "module": {
          "description": "Legacy alias for modulePath (deprecated)",
          "type": "string"
        },
        "modulePath": {
          "description": "Module identifier — EDT module path (CommonModules/Foo/Module.bsl) or absolute file path (required)",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name (required when modulePath is module-relative)",
          "type": "string"
        }
      },
      "required": [
        "lineNumber"
      ],
      "type": "object"
    },
    "name": "set_breakpoint",
    "outputSchema": {
      "properties": {
        "breakpointId": {
          "description": "Eclipse marker id of the created breakpoint (-1 if none)",
          "type": "integer"
        },
        "degraded": {
          "description": "True when only a marker-only breakpoint could be created",
          "type": "boolean"
        },
        "lineNumber": {
          "description": "1-based line number where the breakpoint was set",
          "type": "integer"
        },
        "module": {
          "description": "Legacy alias echo of the module identifier",
          "type": "string"
        },
        "modulePath": {
          "description": "Module identifier as supplied (EDT path or absolute path)",
          "type": "string"
        },
        "resolvedFile": {
          "description": "Workspace-relative path of the resolved .bsl file",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "warning": {
          "description": "Warning text when the breakpoint is degraded/marker-only",
          "type": "string"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Store infobase connection credentials (user/password) so update_database and debug_launch can authenticate the update agent on an infobase that has a user list (issue #194). Selects an EXISTING infobase user (does not create users); an empty password is valid (demo bases). Target by launchConfigurationName (preferred) or projectName + applicationId (from get_applications). Full parameters and examples: call get_tool_guide('set_infobase_credentials').",
    "inputSchema": {
      "properties": {
        "access": {
          "description": "Authentication kind: 'INFOBASE' (default, 1C user auth) or 'OS' (OS authentication).",
          "enum": [
            "INFOBASE",
            "OS"
          ],
          "type": "string"
        },
        "applicationId": {
          "description": "Application ID from get_applications; required if launchConfigurationName is omitted.",
          "type": "string"
        },
        "launchConfigurationName": {
          "description": "Exact runtime-client config name from list_configurations (preferred target).",
          "type": "string"
        },
        "password": {
          "description": "Infobase user password. Optional; default empty (demo bases use an empty password).",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name; required if launchConfigurationName is omitted.",
          "type": "string"
        },
        "user": {
          "description": "Infobase user name to authenticate as (an EXISTING user). Optional: empty stores no-user credentials (OS-authenticated or userless base / reset).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "set_infobase_credentials",
    "outputSchema": {
      "properties": {
        "access": {
          "description": "Stored access kind (INFOBASE or OS).",
          "type": "string"
        },
        "applicationId": {
          "description": "Target application ID.",
          "type": "string"
        },
        "applicationName": {
          "description": "Display name of the target application.",
          "type": "string"
        },
        "message": {
          "description": "Human-readable status message.",
          "type": "string"
        },
        "passwordSet": {
          "description": "True when a non-empty password was stored.",
          "type": "boolean"
        },
        "project": {
          "description": "Target EDT project name.",
          "type": "string"
        },
        "success": {
          "description": "Whether the credentials were stored",
          "type": "boolean"
        },
        "user": {
          "description": "Stored infobase user name.",
          "type": "string"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Start performance measurement on the active debug target. Enables line-level profiling: call counts and timing for every executed BSL line. Start-only and idempotent: if profiling is already active for this applicationId it stays on. Call stop_profiling to stop, then get_profiling_results to see which code was covered. Requires an active debug session (debug_launch or debug_yaxunit_tests).",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application id of the running debug session (required)",
          "type": "string"
        }
      },
      "required": [
        "applicationId"
      ],
      "type": "object"
    },
    "name": "start_profiling",
    "outputSchema": {
      "properties": {
        "active": {
          "description": "Whether profiling is now active for this applicationId",
          "type": "boolean"
        },
        "applicationId": {
          "description": "Application id of the profiled debug session",
          "type": "string"
        },
        "message": {
          "description": "Human-readable status and next-step guidance",
          "type": "string"
        },
        "started": {
          "description": "True if this call started profiling, false if already active",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Step a suspended debug thread. kind ∈ {over, into, out}. Blocks until the next SUSPEND event (or timeout) and returns the new frame snapshot.",
    "inputSchema": {
      "properties": {
        "kind": {
          "description": "Step kind: over, into, out (required)",
          "enum": [
            "over",
            "into",
            "out"
          ],
          "type": "string"
        },
        "threadId": {
          "description": "Thread id from wait_for_break (required)",
          "type": "integer"
        },
        "timeout": {
          "description": "Wait window in seconds (default: 30)",
          "type": "integer"
        }
      },
      "required": [
        "threadId",
        "kind"
      ],
      "type": "object"
    },
    "name": "step",
    "outputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application id of the debug session",
          "type": "string"
        },
        "frames": {
          "description": "Stack frames: frameIndex, frameRef, name, line, modulePath, project",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "hit": {
          "description": "True if a suspend event was caught, false on timeout",
          "type": "boolean"
        },
        "reason": {
          "description": "Reason when not hit (e.g. timeout)",
          "type": "string"
        },
        "serverTarget": {
          "description": "True if stepping a 1C debug-server target",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "threadId": {
          "description": "Id of the suspended thread",
          "type": "integer"
        },
        "threadName": {
          "description": "Name of the suspended thread",
          "type": "string"
        },
        "topFrameRef": {
          "description": "Stable ref of the top stack frame",
          "type": "integer"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Stop performance measurement on the active debug target. Counterpart to start_profiling: deterministically switches profiling off. Idempotent: if profiling is not active for this applicationId it returns a benign result, not an error. Call get_profiling_results afterwards to retrieve the collected coverage.",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application id of the running debug session (required)",
          "type": "string"
        }
      },
      "required": [
        "applicationId"
      ],
      "type": "object"
    },
    "name": "stop_profiling",
    "outputSchema": {
      "properties": {
        "active": {
          "description": "Whether profiling is still active after the call",
          "type": "boolean"
        },
        "applicationId": {
          "description": "Application id the result refers to",
          "type": "string"
        },
        "message": {
          "description": "Human-readable summary of the outcome",
          "type": "string"
        },
        "stopped": {
          "description": "Whether profiling was actually toggled off",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Terminate one or more 1C launches started from THIS EDT instance; externally launched 1C clients are never touched. Select ONE target mode: launchConfigurationName, projectName+applicationId, or all=true (needs confirm=true). Full parameters and examples: call get_tool_guide('terminate_launch').",
    "inputSchema": {
      "properties": {
        "all": {
          "description": "Terminate every live EDT launch (optionally narrowed by projectName); requires confirm=true. Default false.",
          "type": "boolean"
        },
        "applicationId": {
          "description": "Application ID from get_applications; requires projectName.",
          "type": "string"
        },
        "confirm": {
          "description": "Required (true) when all=true; guards against accidental mass termination.",
          "type": "boolean"
        },
        "force": {
          "description": "On polite-termination timeout, escalate to an OS-level process kill; may lose unsaved 1C state. Default false. Ignored for Attach.",
          "type": "boolean"
        },
        "includeAttach": {
          "description": "Whether to act on Attach configs (disconnected, server keeps running). Default true; set false to skip them.",
          "type": "boolean"
        },
        "launchConfigurationName": {
          "description": "Exact launch configuration name from list_configurations (single-launch mode).",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name; pair with applicationId for one launch, or with all=true.",
          "type": "string"
        },
        "timeout": {
          "description": "Polite-wait window per launch in seconds, clamped to [1, 120]. Default from EDT preferences (factory default 10).",
          "type": "integer"
        },
        "timeoutSeconds": {
          "description": "Deprecated alias of 'timeout' (kept for backward compatibility).",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "terminate_launch",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Run EDT 'Translate configuration' on a configuration project - reads the dictionaries from the storages bound to it (external dictionary storage projects with the dependentProjectNature, or in-configuration storages) and regenerates the translated artifacts. Equivalent of the context-menu action Translation -> Translate configuration. Requires LanguageTool installed in EDT.",
    "inputSchema": {
      "properties": {
        "projectName": {
          "description": "Project name (typically the source, e.g. the ru project). Required.",
          "type": "string"
        },
        "targetLanguages": {
          "description": "Target language codes to synchronize (e.g. [\"en\"]). Required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "projectName",
        "targetLanguages"
      ],
      "type": "object"
    },
    "name": "translate_configuration",
    "outputSchema": null
  },
  {
    "annotations": {
      "destructiveHint": true,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Apply configuration changes to an application's database (infobase), full or incremental. Target by launchConfigurationName (preferred) or projectName + applicationId. Destructive/irreversible: guarded by a confirm-preview - call without confirm to preview the exact update (no infobase change), then confirm=true to apply. Auto-terminates any 1C client THIS EDT launched on the target infobase first to free the exclusive lock (opt out with terminateRunningClients=false). Full parameters and examples: call get_tool_guide('update_database').",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application ID from get_applications; required if launchConfigurationName is omitted.",
          "type": "string"
        },
        "confirm": {
          "description": "true = apply the update; default false = preview only (resolves the target and reports what would change WITHOUT mutating the infobase).",
          "type": "boolean"
        },
        "fullUpdate": {
          "description": "true = full reload, false = incremental (default false).",
          "type": "boolean"
        },
        "launchConfigurationName": {
          "description": "Exact runtime-client config name from list_configurations (preferred target).",
          "type": "string"
        },
        "projectName": {
          "description": "EDT project name; required if launchConfigurationName is omitted.",
          "type": "string"
        },
        "terminateRunningClients": {
          "description": "Before applying, terminate any 1C client THIS EDT launched on the target infobase to free the exclusive lock (default true). false keeps a running client — the update then fails if that client holds the infobase exclusively.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "update_database",
    "outputSchema": {
      "properties": {
        "action": {
          "description": "Either 'preview' (nothing changed) or 'updated' (applied).",
          "type": "string"
        },
        "applicationId": {
          "description": "Target application ID.",
          "type": "string"
        },
        "applicationName": {
          "description": "Display name of the target application.",
          "type": "string"
        },
        "confirmationRequired": {
          "description": "true on a preview (no infobase change made); absent/false once updated.",
          "type": "boolean"
        },
        "message": {
          "description": "Human-readable status message for the update.",
          "type": "string"
        },
        "project": {
          "description": "Target EDT project name.",
          "type": "string"
        },
        "stateAfter": {
          "description": "Application update state after the update.",
          "type": "string"
        },
        "stateBefore": {
          "description": "Application update state before the update.",
          "type": "string"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "terminatedClient": {
          "description": "Present and true ONLY when an applied update (confirm=true) terminated a running client to free the infobase; absent otherwise (preview, opt-out, or no running client).",
          "type": "boolean"
        },
        "updateType": {
          "description": "Update mode applied: FULL or INCREMENTAL.",
          "type": "string"
        },
        "willTerminateRunningClients": {
          "description": "On a preview: whether confirm=true would first terminate a running client (reflects terminateRunningClients).",
          "type": "boolean"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Validate 1C:Enterprise query language (QL) text against a project, returning syntax and semantic errors with line numbers. Use to check a query before embedding it in BSL; resolves table/field names against the project's metadata. Full parameters and examples: call get_tool_guide('validate_query').",
    "inputSchema": {
      "properties": {
        "dcsMode": {
          "description": "true for Data Composition System queries (allows DCS-specific syntax). Default: false.",
          "type": "boolean"
        },
        "projectName": {
          "description": "EDT project name (required).",
          "type": "string"
        },
        "queryText": {
          "description": "Full 1C query text to validate (required), e.g. 'SELECT Ref FROM Catalog.Products'.",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "queryText"
      ],
      "type": "object"
    },
    "name": "validate_query",
    "outputSchema": {
      "properties": {
        "dcsMode": {
          "description": "Whether DCS validation mode was used",
          "type": "boolean"
        },
        "errorCount": {
          "description": "Number of ERROR-severity issues",
          "type": "integer"
        },
        "infoCount": {
          "description": "Number of INFO-severity issues",
          "type": "integer"
        },
        "issues": {
          "description": "Validation issues; each has severity, message, optional line/column/offset",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "valid": {
          "description": "true when the query has zero issues",
          "type": "boolean"
        },
        "warningCount": {
          "description": "Number of WARNING-severity issues",
          "type": "integer"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Wait for a debug suspend event (e.g. breakpoint hit) on the given application. Returns the suspended thread/frame snapshot, or {hit:false} on timeout. applicationId accepts ANY id form for the session: the real id, 'attach:<name>', 'launch:<name>' (EDT-UI-started session), or 'ServerApplication.<app>' (server-side suspend from debug_yaxunit_tests). If omitted and exactly one EDT debug session is active, that session is used. Does NOT terminate the launch on timeout — call again to keep waiting.",
    "inputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application id of the running debug session (real, 'attach:<configName>', 'launch:<configName>', or 'ServerApplication.<app>'). Optional if exactly one debug session is active.",
          "type": "string"
        },
        "timeout": {
          "description": "Wait window in seconds (default: 60)",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "wait_for_break",
    "outputSchema": {
      "properties": {
        "applicationId": {
          "description": "Application id of the debug session waited on",
          "type": "string"
        },
        "autoResolved": {
          "description": "True if applicationId was auto-resolved",
          "type": "boolean"
        },
        "frames": {
          "description": "Stack frames of the suspended thread (frameIndex, frameRef, name, line, modulePath, project)",
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "hit": {
          "description": "True if a suspend was observed, false on timeout",
          "type": "boolean"
        },
        "reason": {
          "description": "Reason when no suspend occurred (e.g. 'timeout')",
          "type": "string"
        },
        "serverTarget": {
          "description": "True if resolved via the 1C debug-server target bridge",
          "type": "boolean"
        },
        "success": {
          "description": "Whether the operation succeeded",
          "type": "boolean"
        },
        "threadId": {
          "description": "Id of the suspended thread",
          "type": "integer"
        },
        "threadName": {
          "description": "Name of the suspended thread",
          "type": "string"
        },
        "topFrameRef": {
          "description": "Stable frame reference of the top stack frame",
          "type": "integer"
        }
      },
      "required": [
        "success"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {
      "destructiveHint": false,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Write BSL source code to a 1C metadata object module. Use to edit a module: searchReplace a fragment (default, needs oldSource), replace the whole file, or append. Target the module by EITHER modulePath OR objectName (mutually exclusive — pass exactly one). Runs a BSL syntax check before writing (skipSyntaxCheck=true to force). Full parameters and examples: call get_tool_guide('write_module_source').",
    "inputSchema": {
      "properties": {
        "commandName": {
          "description": "Command name; required when moduleType=CommandModule (e.g. 'FillByTemplate').",
          "type": "string"
        },
        "expectedHash": {
          "description": "Lost-update guard for any mode: the contentHash from your last read; mismatch rejects.",
          "type": "string"
        },
        "expectedSource": {
          "description": "Lost-update guard for mode=replace: the module content you last read; mismatch rejects.",
          "type": "string"
        },
        "formName": {
          "description": "Form name; required when moduleType=FormModule (e.g. 'ItemForm').",
          "type": "string"
        },
        "mode": {
          "description": "Write mode (default searchReplace).",
          "enum": [
            "searchReplace",
            "replace",
            "append"
          ],
          "type": "string"
        },
        "modulePath": {
          "description": "src/-relative module path, e.g. 'CommonModules/MyModule/Module.bsl'. Alternative to objectName (pass exactly one).",
          "type": "string"
        },
        "moduleType": {
          "description": "Module type for objectName resolution (default ObjectModule).",
          "enum": [
            "ObjectModule",
            "ManagerModule",
            "FormModule",
            "CommandModule",
            "RecordSetModule",
            "Module"
          ],
          "type": "string"
        },
        "objectName": {
          "description": "Object name 'Type.Name', e.g. 'Document.MyDoc'. Resolved with moduleType. Alternative to modulePath.",
          "type": "string"
        },
        "oldSource": {
          "description": "Fragment to find and replace; required for searchReplace, must match exactly once.",
          "type": "string"
        },
        "overwrite": {
          "description": "Force mode=replace over an existing module without an expectedSource check (default false).",
          "type": "boolean"
        },
        "projectName": {
          "description": "EDT project name (required)",
          "type": "string"
        },
        "skipSyntaxCheck": {
          "description": "Skip the BSL syntax check (default false).",
          "type": "boolean"
        },
        "source": {
          "description": "BSL source to write (required): full file for replace, new fragment for searchReplace, text to add for append.",
          "type": "string"
        }
      },
      "required": [
        "projectName",
        "source"
      ],
      "type": "object"
    },
    "name": "write_module_source",
    "outputSchema": null
  }
]
